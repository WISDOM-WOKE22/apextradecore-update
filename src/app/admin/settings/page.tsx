"use client";

import { SettingsView } from "@/components/Settings";

export default function AdminSettingsPage() {
  return (
    <SettingsView
      title="Admin Settings"
      subtitle="Configure your admin account and password."
    />
  );
}
